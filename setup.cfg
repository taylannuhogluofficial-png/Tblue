[tool:pytest]
testpaths = tests
addopts = -p no:terminal -n auto --tb=short

[coverage:run]
source = tblue
omit =
    tests/*
    setup.py

[coverage:report]
show_missing = True
skip_covered = False
fail_under = 80

[coverage:html]
directory = htmlcov
